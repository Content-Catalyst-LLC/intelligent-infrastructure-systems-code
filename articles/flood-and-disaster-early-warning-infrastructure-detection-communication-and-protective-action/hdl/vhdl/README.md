# VHDL Placeholder

This directory is reserved for VHDL implementations of early-warning threshold detection, sensor-stream validation, or FPGA edge-processing modules.

The current hardware example is implemented in Verilog under `hdl/verilog/`.
